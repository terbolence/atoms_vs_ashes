<!-- Generated by scripts/report_effort_metrics.py -->
# Effort metrics report

**Generated (UTC):** 2026-04-14T15:14:27Z

## `# man_hours:` line headers

Convention: first-line `# man_hours: N.N` in source files (engineering-time estimate).

| Scope | Files tagged | Sum (hours) |
| --- | ---: | ---: |
| `src/` | 139 | 505.5 |
| `tests/` | 50 | 141.1 |
| `alembic/` | 8 | 24.5 |
| `scripts/` | 7 | 25.5 |
| **Total** | **204** | **696.6** |

> For registry-based totals see `audit/man_hours_registry.yml` and `scripts/man_hours_report.py`.

## Lines of code (Python, `wc` on `*.py`)

*Omitted by default (chunked `wc` on cloud-backed trees can stall). Re-run with `--loc` for tracked `*.py` line counts, or use `cloc --with-cloc`.*

## REST request JSON logs

*Omitted by default (large or cloud-synced `logs/`). Re-run with `--scan-logs`.*

## LLM usage from local logs

*Omitted unless `--scan-logs` is set.*

---

*Regenerate: `python scripts/report_effort_metrics.py --write-report` (`--loc`, `--with-cloc`, `--scan-logs` as needed)*
