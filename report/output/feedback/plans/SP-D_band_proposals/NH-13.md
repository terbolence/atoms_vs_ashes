<!-- man_hours: 0.4 -->
---
criterion_id: NH-13
family: nh
rubric_file: config/scoring_rubrics/nh_natural_hazards.yaml
sign_off: no
sign_off_by: ""
sign_off_at: ""
data_sanity_verdict: data_needs_fix_before_band_edit
audit_id_alias: null
fb_lls_honored: [FB-LL-01, FB-LL-08, FB-LL-11]
project_lls_honored: [LL-022]
reviewer_comments: ['#101', '#574']
depends_on: [phase04_feedback_lessons, phase05_data_audit]
---

# Phase 0.6 — Band proposal: NH-13 Forest / wildfire

## 1. Reviewer expectation

- **#101 (Timelkam, AT)**: anchor "score 5.0/10". Reviewer: "scor prea mic – trebuie sa fii aproape de o padure sau vegetatie abundenta" — "Score too low — you need to be close to a forest or abundant vegetation."
- **#574 (Braila, RO)**: anchor "Forest/Wildfire (NH-13) - score 5.0/10 (MC 5.0-5.0), weight 0.0132, data quality n/a. Evidence: values not in measurement tables." Reviewer: "Score too small since Nuscale standard design is for Tornado of cat IV". **FB-LL-11 anchor mismatch**: reviewer's text refers to **tornado** (NH-10 winds territory), not wildfire. Treat as separate clarify-with-reviewer item.

Reviewer's mental model (interpreting #101 — the substantive wildfire comment): when there is **no nearby forest / combustible vegetation**, the score should be `[9,10]`. The rubric's high band already says `combustible_veg_pct < 5` → `[9,10]`, so the issue is that the data is NULL and the score falls through.

## 2. Current rubric (snapshot)

| Score range | Condition | Descriptor |
| --- | --- | --- |
| [9,10] | `combustible_veg_pct < 5` | < 5% forest/grassland; no fire scars. |
| [7,8] | `combustible_veg_pct < 15` | 5-15%; < 1 large fire/decade. |
| [5,6] | `combustible_veg_pct < 35` | 15-35%; 1-3 fires/decade. |
| [3,4] | `combustible_veg_pct <= 60` | 35-60%; 3-6 fires/decade. |
| [1,2] | `combustible_veg_pct > 60` | > 60%; > 6 fires/decade. |

## 3. Data sanity verdict

Phase 0.5 → `data_needs_fix_before_band_edit`. **All five wildfire columns are 100% NULL**: `wildfire_combustible_pct`, `wildfire_wui_ha`, `nh13_gee_modis_burn_months`, `nh13_gee_fire_recurrence_class`, `nh13_gee_burn_fraction_mean`. The connector for wildfire is **non-functional** at the moment.

This is the worst-case LL-022 silent-zero variant: the column is NULL, not zero, but the rubric defaults all 361 sites to "unscored → engine pass-mark 5.0".

## 4. Diagnosis

The rubric is correct in design. The data pipeline is broken — there is no connector currently populating `combustible_veg_pct` (or it is populating under a different column name and the rubric variable resolution is missing it). Until the connector is fixed (SP-F), no rubric edit can satisfy reviewer #101.

Two options:

1. **Wait for SP-F connector fix** (preferred). The current rubric thresholds are already reviewer-aligned ("< 5% combustible veg → high score" matches reviewer mental model).
2. **Add an LLM-fallback OR-clause** so `nh13_quality = 'low_silent_zero'` and the LLM-derived narrative ("This site sits in agricultural floodplain with no nearby forest" — for Timelkam type) can supply a textual high-band justification.

For the reviewer-anchor mismatch on #574, **route to clarify-with-reviewer**; do not action it as either NH-13 or NH-10 until the reviewer confirms intent.

## 5. Proposed bands

**No numeric threshold change.** Add an explicit unscored-guard that prevents the engine pass-mark default and routes via the LLM fallback when all combustible-veg columns are NULL.

| Score range | Condition (PROPOSED) | Delta vs current | Descriptor |
| --- | --- | --- | --- |
| [9,10] | `combustible_veg_pct < 5` | unchanged | < 5% forest/grassland; no fire scars. |
| [7,8] | `combustible_veg_pct < 15` | unchanged | 5-15%; < 1 large fire/decade. |
| [5,6] | `combustible_veg_pct < 35` | unchanged | 15-35%; 1-3 fires/decade. |
| [3,4] | `combustible_veg_pct <= 60` | unchanged | 35-60%; 3-6 fires/decade. |
| [1,2] | `combustible_veg_pct > 60` | unchanged | > 60%; > 6 fires/decade. |

**Engine-side change** (already covered by SP-E renderer): when `combustible_veg_pct IS NULL`, the criterion is rendered as "no native score (unscored — wildfire connector pending)" with the LLM narrative used as best-available qualitative evidence. The score is **not asserted as 5.0**.

## 6. Boundary-example matrix (FB-LL-08)

| Site | combustible_pct | Current | Proposed | Reviewer expectation |
| --- | --- | --- | --- | --- |
| Timelkam (AT) — anchor #101 | NULL (true value ~3% in agricultural plain) | 5.0 (unscored falls to pass-mark; renderer says "values not in measurement tables") | unscored; renderer says "no native score (unscored — wildfire connector pending). LLM narrative: agricultural plain, no nearby forest" | high (>=9) — but **only after SP-F lands** |
| Forest-edge site (CZ Sumava) | 65 (synthetic; would be the case after connector fix) | not yet computed | `[1,2]` matches → 1.5 | low ✅ |
| RO Braila (#574 — wildfire) | NULL | 5.0 | unscored | high (`combustible_veg_pct ~5%` after connector lands) |
| RO Braila (#574 — tornado, if reviewer-confirmed reroute to NH-10) | (NH-10 territory) | NH-10 unchanged | reroute to NH-10 | clarify with reviewer first |

## 7. Acceptance tests / regression

- **Anchor #101 (Timelkam)**: NH-13 must read "no native score (unscored)" in the rendered profile until the wildfire connector lands. After SP-F fix, Timelkam must be in `[9,10]`.
- **Anchor #574 (Braila wildfire)**: same as above for the wildfire reading; reroute to NH-10 only after reviewer confirms intent.
- **No site asserted at 5.0**: zero rendered NH-13 bullets show "5.0/10 — values not in measurement tables".

## 8. Open questions for reviewer

- **Q-NH-13-A**: confirm SP-F priority for the wildfire connector. The current rubric is sound; the data pipeline is the bottleneck. **Default-recommend high priority for SP-F**.
- **Q-NH-13-B (FB-LL-11)**: confirm intent of comment #574 — wildfire (NH-13) or tornado/wind (NH-10)? **Action: ask reviewer in next sync.** Until confirmed, no rubric edit either way.

## 9. Hand-off note for SP-D

This proposal is a **no-op on the rubric YAML**. SP-D files this as `sign_off: yes` once the user acknowledges that the fix path is connector-side (SP-F) plus renderer-side (SP-E, already done).
