<!-- man_hours: 0.4 -->
---
criterion_id: EP-01
family: ep
rubric_file: config/scoring_rubrics/ep_emergency_planning.yaml
sign_off: yes
sign_off_by: user
sign_off_at: 2026-05-09
data_sanity_verdict: data_clean
audit_id_alias: null
fb_lls_honored: [FB-LL-02]
project_lls_honored: [LL-026]
reviewer_comments: []
depends_on: [phase04_feedback_lessons]
---

# Phase 0.6 — Band proposal: EP-01 Emergency-plan feasibility (composite)

## 1. Reviewer expectation

No EP-01 reviewer comment in the 45-comment set. Proposal is **non-substantive** — confirm bands as-is, document that the data is clean, and add the FB-LL-02 unscored-handling clause in case `ep01_composite_score` is NULL on a future run.

## 2. Current rubric (snapshot)

| Score range | Condition | Descriptor |
| --- | --- | --- |
| [9,10] | `ep01_composite_score >= 85` | >= 85. |
| [7,8] | `ep01_composite_score >= 70` | 70-84. |
| [5,6] | `ep01_composite_score >= 55` | 55-69. |
| [3,4] | `ep01_composite_score >= 40` | 40-54; severe EP ranking penalty, not exclusionary by itself. |
| [1,2] | `ep01_composite_score >= 30` | 30-39; borderline EP feasibility, ranking penalty unless E8 hard expression triggers. |
| [0,0] | `ep01_composite_score < 30 or nearest_trauma_center_km > 60` | E8 confirmed. |

`fail_conditions`: E8 (exclude) on composite < 30 OR trauma centre > 60 km; `hospital_overlay` (screen_flag) on hospital > 25 km.

## 3. Data sanity verdict

Phase 0.5 → `data_clean`. `ep01_composite_score`, `nearest_hospital_km`, `nearest_trauma_center_km` all populated on >95% of sites; no high-null fraction; no LL-022 silent-zero pattern.

## 4. Diagnosis

Nothing reviewer-flagged on this criterion. The bands are calibrated against the DRV-02 / E8 thresholds and match the chapter 6 methodology. The only latent issue is FB-LL-02: if `ep01_composite_score` is ever NULL (e.g. a brand-new connector run that has not yet computed the composite), the engine currently falls through to the pass-mark default `[5,6]` and the renderer prints "5.0/10" alongside "values not in measurement tables".

## 5. Proposed bands

**No change to numeric thresholds.** Add an explicit unscored guard that prevents the `[5,6]` pass-mark default from being asserted when `ep01_composite_score IS NULL`. This is a defensive change, not a re-banding.

| Score range | Condition (PROPOSED) | Delta vs current | Descriptor |
| --- | --- | --- | --- |
| [9,10] | `ep01_composite_score >= 85` | unchanged | >= 85. |
| [7,8] | `ep01_composite_score >= 70` | unchanged | 70-84. |
| [5,6] | `ep01_composite_score >= 55` | unchanged | 55-69. |
| [3,4] | `ep01_composite_score >= 40` | unchanged | 40-54; severe EP ranking penalty, not exclusionary by itself. |
| [1,2] | `ep01_composite_score >= 30` | unchanged | 30-39; borderline EP feasibility, ranking penalty unless E8 hard expression triggers. |
| [0,0] | `ep01_composite_score < 30 or nearest_trauma_center_km > 60` | unchanged | E8 confirmed. |

**No new branches; no thresholds moved.** The unscored-guard is implemented engine-side (already covered by SP-E) and does not need a rubric clause.

## 6. Boundary-example matrix (FB-LL-08)

| Case | Inputs | Current behavior | Proposed behavior |
| --- | --- | --- | --- |
| Composite 87, hospital 12 km, trauma 35 km | composite_score=87, hospital_km=12, trauma_km=35 | `[9,10]` matches → 9.5 | unchanged → 9.5 |
| Composite 60, hospital 18 km, trauma 25 km | composite_score=60, hospital_km=18, trauma_km=25 | `[5,6]` matches → 5.5 | unchanged → 5.5 |
| Composite NULL, hospital NULL, trauma NULL | all NULL | falls through → `[5,6]` default 5.0 (but renderer now flags unscored, SP-E) | rubric unchanged; renderer says "no native score (unscored — composite missing)" |
| Composite 25, hospital 30 km, trauma 70 km | composite_score=25, hospital_km=30, trauma_km=70 | E8 fires → exclude | unchanged → exclude |

## 7. Acceptance tests / regression

- Re-run scoring against the 18-anchor regression matrix; EP-01 scores must be **identical** to the pre-proposal run on every site where `ep01_composite_score` is non-NULL.
- For any site where `ep01_composite_score IS NULL`, the rendered profile bullet must read "no native score (unscored — composite missing)" rather than "5.0/10 — values not in measurement tables".

## 8. Open question for reviewer

None. This proposal is a no-op on the numeric rubric and only documents the unscored-guard contract for completeness.

## 9. Hand-off note for SP-D

This file is **a documentation-only proposal**; SP-D may flip `sign_off: yes` and proceed with no rubric YAML edit if the user concurs. The unscored-guard is already implemented in SP-E and does not need an SP-D commit.


## Verification (2026-05-09)

The offline regression for this band proposal runs through the rubric/spec test suite.

- `PYTHONPATH=src .venv/bin/pytest tests/scoring/ -q` -> **89/89 green** as of 2026-05-09 17:11 UTC, with this criterion's bands already reflected in [`config/scoring_rubrics/`](../../../../config/scoring_rubrics/) and the [`config/scoring_specs/`](../../../../config/scoring_specs/) parity-mirror.
- The 18-anchor on-DB regression matrix is deferred to **Stage 8b SP-G scoring rerun** (`run_id = feedback_rerun_<date>`), which will diff each anchor's composite under the new bands against the prior `run_id`. Per-criterion predicted-vs-observed deltas land in the SP-G rerun audit before the country / site profiles are regenerated.
- Sign-off: see frontmatter (`sign_off: yes` 2026-05-09).
