<!-- man_hours: 0.6 -->
# NH-02 Threshold Menu Source Of Truth

Status: Completed

Execution notes: implemented by resolving recipe-linked default pivots from user-editable threshold metadata before static `band_recipe.score5_pivot`, aligning `NH-02` spec defaults to 8 km, and adding preview/compiler/runtime tests proving 8-default and 5-override behavior.

## Task

Make the Site Selection Criteria threshold input for `NH-02 / E1` the source of truth for the compiled scoring behavior. If the menu shows 8 km, `NH-02` must compile and score using an 8 km score-5 / hard-exclusion pivot. If it shows 5 km, the compiled bands and exclusion must use 5 km.

## Current State

- `config/scoring_specs/nh_natural_hazards.yaml` declares `NH-02` with `band_recipe.score5_pivot: 5.0` and `derive_expr_from_recipe: true`.
- `config/scoring_specs/threshold_metadata.yaml` declares `NH-02/E1` with `default_value: 8.0` and recommended value 8.0.
- The GUI preview currently displays the threshold metadata value, while the compiler falls back to `band_recipe.score5_pivot` when no explicit override exists.
- This creates a mismatch: the user-visible menu can show 8 km while the engine compiles the hidden 5 km pivot.

## Desired Behavior

- For recipe-linked user-editable thresholds, the resolved threshold control value must drive:
  - the score-5 band boundary;
  - the lower/higher surrounding bands generated from the recipe;
  - the hard exclusion expression generated by `derive_expr_from_recipe`.
- Explicit saved user overrides still win over metadata defaults.
- Tests must prove the GUI preview value and compiler output stay aligned for `NH-02`.

## Ordered Steps

1. Open the required Feature Completion Matrix under `audit/feature_completion_matrices/`.
2. Update the compiler pivot resolution so user-editable threshold metadata default values take precedence over static recipe pivots when no saved override exists.
3. Keep the drift guard focused on YAML self-consistency where appropriate, without rejecting the intentional metadata-driven default for `NH-02`.
4. Update preview or tests so the Site Selection Criteria value visibly matches the compiled `NH-02` band and exclusion expression.
5. Add/update an entry-point-facing GUI/preview test that would fail if the editor value stayed at 8 while the compiler used 5.
6. Run focused tests and lints for touched files.
7. Update project audit artifacts: plan mirrors, conversation log, and man-hours registry.
